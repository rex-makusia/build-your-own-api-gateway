server:
  port: 8080
  read_timeout: 5s
  write_timeout: 10s

logging:
  level: info
  format: json

backend_services:
  service1:
    url: http://localhost:3001
    timeout: 2s
  service2:
    url: http://localhost:3002
    timeout: 2s

auth:
  jwt_secret: your_jwt_secret
  token_expiry: 24h